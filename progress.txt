2026-06-30
- 工作台 V1.1 终态结果包提交链路修复完成，版本提升到 v2.0.59：修复“插件任务进度到 100%，但服务端没有 RawSnapshot/RawRecord”的结果提交断层。任务完成、失败、停止、超时、结果包丢失、结构化记录被过滤为空时，插件都会先通过 `/api/execution-stations/sync` 提交 `commit_raw_snapshot` 终态包；即使 records 为空，也会给服务端一个可入库的 RawSnapshot。失败/停止分支改为先 `deferRelease`，提交终态包成功后再释放任务，避免先释放执行权导致服务端不再承认最终包；终态包暂时提交失败时，插件保留本地租约和执行上下文，下一轮继续补交。`taskLeaseClient` 返回 `clientRecordStats`，能看到输入记录数、提交记录数、过滤记录数和过滤原因。同步协议文档已更新。发布包 `releases/linggan-boom-v2.0.59.zip` 已生成并同步内容工作台托管下载位，SHA256 为 `61873f6c39953c551c662206107760b2642a5927dd62e454cc2d712f3d73c60b`，大小 `418284` 字节。验证：`node --test tests/workbench-task-lease.test.mjs tests/workbench-task-poller.test.mjs` 71/71 通过；`npm run check:contracts` 通过；`npm run test:douyin` 224/224 通过；`npm run build`、`npm run release:verify -- --version 2.0.59 --zip releases/linggan-boom-v2.0.59.zip` 通过。

2026-06-29
- 本地插件源仓同步到 v2.0.55，并把“本地仓库永远为最新”作为执行铁律落实到实际文件：`manifest.json`、`package.json`、`package-lock.json`、`dist/manifest.json`、根目录 `dist.zip`、`releases/linggan-boom-v2.0.55.zip` 全部对齐到 `2.0.55`；内容工作台 `plugin-releases/linggan-boom-latest.zip` 也改为同一份源仓 release 包，SHA256 `05f8f6d2b029f5b230552e1c96a9e85530321afe5f11766a8bf108ef7da5390f`，大小 `415555` 字节。
  · 派单接单协议从 v2.0.54 的“旧字段并存”推进到 v2.0.55 的本地纯 V1.1 派单段：`/sync` body 不再发送 `authorizationId / pluginAuthorizationId / capabilities / platformAccounts / claimMode / mailboxVersion / localLease / forceFullSync` 等旧字段；授权身份只走 `Authorization: Bearer <authorizationToken>` 请求头。
  · capacity 改为真实 V1.1 车道 key：小红书默认上报 `xhs.monitor_patrol`，抖音默认上报 `douyin.governance`，评论类能力映射到 `xhs.comments / douyin.comments`；不再发送裸 `xhs / douyin` 平台级 key。
  · 接单流程接通 `reservations[] → start_job`：插件第一次 `/sync` 上报容量拿到 reservation 后，会二次 `/sync` 发送 `start_job` operation，携带 `jobId / reserveToken / reservationEpoch` 换取正式 `leaseToken`；任务对象从 reservation.taskSpec 组装，不再依赖旧 claim envelope。
  · 续租改走 V1.1 `progress_update`：运行中任务续租通过 `/api/execution-stations/sync` 发送 `progress_update` operation，不再调用旧 `/api/collection-tasks/:id/lease`。
  · 同步文档：`docs/technical/MESSAGE_PROTOCOL.md` 改为 v2.0.55 纯 V1.1 派单/续租说明；`docs/product/PRD.md` 的自动接单链路和拓扑从 `claim/lease` 改为 `reservation/start_job/progress_update`。
  · 验证：`node --test tests/sync-envelope-v11.test.mjs tests/workbench-task-lease.test.mjs` 34/34 通过；`npm run build` 通过（仅既有 webpack 包体积 warning）；`npm run release:verify -- --version 2.0.55 --zip releases/linggan-boom-v2.0.55.zip` 通过；zip 内 manifest 校验为 2.0.55。已知剩余：结果写回仍走 `/api/collection-tasks/:taskId/ingest`，后续需迁到 V1.1 raw snapshot commit 后再删除旧 ingest 入口。

2026-06-27
- 内容工作台 V1.1 采集架构重构 — 插件 /sync 协议字段补齐（v2.0.54）：内容工作台 V1.1 重构把 `/api/execution-stations/sync` 升级为手册第 5.5 节完整协议（服务端 execution-sync-service.ts 1238 行已落地），插件需配合发送 V1.1 新字段。
  · 新增 V1.1 envelope builder：`src/workbench/protocol/syncEnvelopeV11.js`（约 380 行，导出 buildSyncRequestV11 / buildMailboxCursors / buildLaneCapacity / buildActiveLeases / buildAccountReports / resolveStationSessionId / clearStationSessionId / extractMailboxVersionsFromResponse / extractNextSyncFromResponse / isV11SyncResponse 等函数）。统一负责 V1.1 字段构造，三处调用方复用。
  · 改造三处调用方：① `executionStationClient.sendHeartbeat`（心跳路径）② `taskLeaseClient.claimCollectionTaskLease`（claim 路径）③ `taskLeaseClient.reconcileExecutionStationLease`（reconcile 路径）。三处现在都通过 buildSyncRequestV11 构造 body，新增字段：protocolVersion="3" / stationSessionId（持久化在 chrome.storage.local） / mailboxCursors（station+lane 对象） / capacity（按 lane 占位 maxReservedTasks=1） / activeLeases[]（数组，从 localLease 转换含 progress/stage/lastProgressAt） / operations[]（恒为空数组，本期不发） / accountReports[]（从 platformAccounts 转换含 healthStatus/cooldownUntil）。
  · 字段并存策略：旧字段（authorizationId/pluginAuthorizationId/capabilities/platformAccounts/claimMode/mailboxVersion/localLease/forceFullSync）全部保留，与 V1.1 字段同时发送。原因：内容工作台 /sync route 直接透传 body 到 service，service 的 SyncRequest 类型不约束额外字段，旧字段被无视但不报错；过渡期插件继续工作，服务端清理旧字段后再去掉。
  · 响应解析双路径兼容：extractMailboxVersionsFromResponse 优先读 V1.1 mailboxVersions 对象，回退到旧 mailbox.version 数字；extractNextSyncFromResponse 优先读 V1.1 nextSync{afterMs,reason} 对象，回退到旧 nextSyncAfterMs 数字。插件 canPollNow 判定改为：V1.1 响应里 mailboxVersions 变化或 nextSync.reason 前缀为 mailbox/claim 时立即触发（替代旧 mode=full_sync 判定）。
  · 错误码识别扩展：executionStationClient.postJson 错误解析里 reasonCode 字段读取顺序调整为 code/reasonCode/error，兼容 V1.1 sync 协议（错误码在 error 字段，如 VERSION_REJECTED）与旧响应（code 字段，如 plugin_protocol_backpressure）。
  · 容量占位策略：capacity 是协议骨架先行，插件没有工时度量基础，按 lane 用 remainingWorkSeconds=0 / targetWorkSeconds=600 / maxReservedTasks=1 占位，使服务端永远判定为需补货（触发 claim）。后续接入真实工时度量后替换为动态值。
  · operations[] 本期恒为空：插件任务结果回传走 /api/collection-tasks/[id]/ingest（Delta Outbox 路径），300017 风控走本地 markCooldown + PATCH 任务状态；这两条路径直连 sync 协议的 commit_raw_snapshot/account_risk_control operation 需要后续大改（不在 V1.1 字段补齐范围内）。服务端 operations 为空时正确处理（nextSync 走 idle 4min）。
  · stationSessionId 持久化：单次插件安装周期内稳定（chrome.storage.local 持久化，跨 service worker 重启保留），换授权/换工位时 clearStationIdentity 内调 clearStationSessionId 清理，让服务端看到新 session。
  · 版本号 bump v2.0.53 → v2.0.54（同步 package.json / package-lock.json / manifest.json）：协议升级是用户可见硬门禁（服务端 V11_MIN_PLUGIN_VERSION=2.0.53 强硬拒绝低于此版本），需发布给所有用户。
  · 文档同步：docs/technical/MESSAGE_PROTOCOL.md 追加 V1.1 /sync 协议字段表 + 响应结构 + 426/401 错误说明。
  · 验证：① 单元测试新增 tests/sync-envelope-v11.test.mjs 22/22 通过（覆盖 buildSyncRequestV11 / session 持久化 / mailboxCursors / capacity / activeLeases / accountReports / 响应解析双路径 / V1.1 识别）；② 端到端模拟脚本 scripts/verify-sync-v11-e2e.mjs 起内存版 V1.1 服务端 mock，让插件 sendHeartbeat 和 claimCollectionTaskLease 真实调用，验证 39 个断言全过（含 protocolVersion/stationSessionId/capacity/activeLeases/accountReports/mailboxCursors 字段齐全 + 响应解析正确 + 426 minPluginVersion 拒绝 + session 持久化）；③ 全套测试 node --test "tests/*.test.mjs" 554/554 通过（原 532 + 新增 22）；④ npm run check:contracts 通过；⑤ npm run build 通过（仅既有 webpack 体积 warning）。
  · 已知断层（如实报告，本期不解决）：① operations[] 本期不发，任务结果回传和风控上报仍走旧 /ingest + PATCH 路径，需要后续接入 commit_raw_snapshot/account_risk_control operation（属于大改）；② capacity 是占位（maxReservedTasks=1 + remainingWorkSeconds=0），不是真实工时度量，服务端 claim 触发判断暂时恒为"需补货"，需要后续接入工时收集；③ account_risk_control operation 不在本期范围，插件 300017 风控仍走纯本地路径（gap 文档 4.2 节），服务端零感知；④ 未做实机联调（需要 Chrome 加载新构建 + 本地工作台 localhost:3000 登录授权），mock 端到端已自验证协议联通，实机验收留给 Mog。
  · 涉及文件：src/workbench/protocol/syncEnvelopeV11.js（新建）/ src/workbench/runtime/executionStationClient.js（改造 sendHeartbeat + clearStationIdentity + postJson 错误码 + 暴露 resolveStationSessionId）/ src/workbench/runtime/taskLeaseClient.js（改造 claimCollectionTaskLease + reconcileExecutionStationLease + 新增 attachMailboxLaneVersions helper）/ src/background/index.js（sendExecutionStationHeartbeat 注入 activeLane/localLease/activeTask）/ tests/sync-envelope-v11.test.mjs（新建）/ scripts/verify-sync-v11-e2e.mjs（新建）/ docs/technical/MESSAGE_PROTOCOL.md（追加 V1.1 协议段）/ package.json + package-lock.json + manifest.json（版本号 bump）/ progress.txt（本条）。

2026-06-25
- 修复"拥堵排队后接单的任务被插件误杀"（result_package_handoff_lost，功能修复）：
  · 症状：崔克西/跟着脑科学家两个 xhs.collectAuthor 任务派出后拥堵 ~3.5h 无人接单（11 次 task.released），10:09 被程烈1/2 接单后 3 分钟内被判 result_package_handoff_lost 失败、0 产出。UI 显示"派出后 3h47m 才失败"，但插件实际只跑了 3 分钟。
  · 根因（生产库 + 代码双轨定位）：插件 taskPoller.js 三处超时判断（LOCAL_ACTIVE_TASK_WITHOUT_LEASE 5min / RUNNING_RESULT_LOOKUP 12min / DISPATCH_STARTUP 45s）都用 activeTask.dispatchedAtMs；而该值在 hydrateTrackedTask(:877) 优先取工作台 task.dispatchedAt，工作台 dispatchedAt 在 lease-service.ts:1455 `dispatchedAt ?? now` 只首次写入不刷新 → 堵车后接单的任务背着"首次派出至今"的虚假超时，采集页第一次轮询找不到结果包就判死，连 12 分钟启动窗口都没给满。dispatchedAt 同时承担 UI"拥堵等待时长"显示，不能改其语义。
  · 修复（最小侵入，仅改插件 taskPoller.js 6 处，不动工作台/数据库/序列化）：新增 activeTask.attemptStartedAtMs = 本轮接单时间。① normalizeActiveTaskShape(:513) 加字段；② hydrateTrackedTask(:886) 赋值优先 matchingPersisted 本地持久化值、fallback task.dispatchedAt/now（不被工作台老值覆盖，因为工作台无此字段）；③ claim 新建 activeTask(:1626) 设 getNow()；④⑤⑥ 三处超时(:1788/:1898/:1959) 从 dispatchedAtMs 换成 attemptStartedAtMs。持久化自动（buildActiveTaskContextSnapshot 用 ...state.activeTask 展开），service worker 重启后从本地恢复。
  · 业界对照（调研 Temporal/SQS/RabbitMQ/Celery/BullMQ/Postgres SKIP LOCKED）：成熟系统把任务生命周期拆成独立计时阶段（Temporal 四超时 ScheduleToStart / StartToClose / ScheduleToClose / Heartbeat；SQS visibility timeout 每轮重置但 DLQ 判断用原始入队时间）。本系统混淆了"排队等待"(ScheduleToStart)和"本轮执行"(StartToClose)两个维度——dispatchedAt 是前者、却被当后者用。本次修复让"本轮执行超时"用"本轮接单时间"，是向 Temporal 模型对齐的第一步。
  · 验证：node --test tests/workbench-task-poller.test.mjs 50/50 通过（含 'result package handoff is lost' 反向用例 + 新增 'congestion-after-claim 不误杀' 正向回归用例——模拟 dispatchedAt 老 3.5h + attemptStartedAtMs 新，断言不被 result_package_handoff_lost 误杀，回退修复即失败）；npm run build 通过（仅既有 webpack 体积 warning）。
  · 未做（待后续）：① 工位实机验收（load unpacked 新构建到程烈1/2，观察后续拥堵任务是否还出现 result_package_handoff_lost）；② 中长期向 Temporal 双层超时模型 fully 对齐（任务级总预算 ScheduleToClose + 真正的 DLQ + 拥堵背压）。
- 文档治理第一批+第二批完成(纯文档,无代码改动,为"内容引擎-灵感爆爆爆"合并做准备):
  · 第一批(低风险归档):根目录 5 份 PLUGIN_REVIEW_*/ACCEPTANCE_CHECKLIST + docs/ 3 份 ACCEPTANCE_REPORT 移到 docs/reviews/(评审类集中);docs/DATA_EXPLORATION_REPORT.md(已被 XHS_FIELD_SURVEY 取代)移 docs/archive/;删除顶层空 docs/ 目录;AGENTS.md 删除 2 处悬空引用(BACKEND_STRUCTURE.md / IMPLEMENTATION_PLAN.md 在仓库中不存在);docs/chrome-web-store/README.md 版本 2.0.19 → 2.0.53(含 SHA256 2396cb16... / size 412601 同步,原 2.0.19 落后 30+ 版本)。
  · 第二批(plans 状态校准 + 建分层索引):task-protocol-draft(草案,协议已实现为 MESSAGE_PROTOCOL.md)+ T11-T13-FIX-SUMMARY(已完成 fix)移 docs/plans/completed/;FIX-SUMMARY-2026-03-26.md 移 archive;修 remediation-execution-plan 状态矛盾(正文标"活跃"但文件在 completed 且 3 月未动 → 改"已归档");新建 docs/README.md 总索引(按 入口/产品/架构/数据/设计/决策/计划/进度/调研/评审/归档/上架 分层 + ✅权威/⚠️滞后/🗄️归档 状态标注,渐进式披露第 2 层);新建 plans/active/README.md(标注每个 active 计划真实状态:6 份 2026-04 接入前计划已落地、留作设计参考)+ plans/completed/README.md + reviews/README.md + archive/README.md;AGENTS.md 权威事实源表补 docs/README.md 导航行。
  · 目的:对齐代码现实 + 渐进式披露(入口 AGENTS.md → 文档地图 docs/README.md → 各层明细),agent 协作时按需深入不必读完所有。纪律:只归档不删除,git 历史可追溯。
  · 未做(待单独确认):第三批入口文件抽公共(AGENTS/CLAUDE 50% 重复,需保留两入口的自动加载能力)。
- 插件授权与自动工位连接重构完成，版本提升到 v2.0.53：把原“先授权、再输入配对码绑定工位”的日常流程改为一次“连接插件”。插件授权码激活和审批领取都会带上本浏览器稳定 `stationKey`，内容工作台自动创建 / 复用工位并返回 `stationId + stationToken`，插件保存后即可心跳和接单；同一账号名下可存在多个浏览器插件，每个插件自动生成一个工位，默认名称按成员和浏览器生成，后续可在工作台改名。Popup 删除日常配对码输入和“绑定工位”按钮，工作台设置页仅保留“旧版配对”作为旧插件或自动连接失败修复入口。下载包引导配置不再携带 `pairingCode`。已同步产品流、PRD、技术协议、测试清单、决策记录、Chrome Web Store 文案和隐私政策。发布包已重新构建并通过校验：`releases/linggan-boom-v2.0.53.zip`、`dist.zip` 已生成，内容工作台 `plugin-releases/linggan-boom-latest.zip` 和 manifest 已刷新到 v2.0.53。验证：插件 `node --test tests/plugin-authorization-client.test.mjs tests/plugin-install-bootstrap.test.mjs tests/popup-ui-copy.test.mjs` 21 个用例通过；插件全量 `node --test tests/*.test.mjs` 531 个用例通过；`npm run check:contracts` 通过；`npm run build` 通过（保留既有 webpack 体积 warning）；`npm run release:verify -- --version 2.0.53 --zip releases/linggan-boom-v2.0.53.zip` 通过；内容工作台自动工位/下载包/设置页定向测试 50 个用例通过，插件下载测试 19 个用例通过；内容工作台 `npm run lint` 通过（保留既有 6 条图片 warning），`npm run build` 通过（保留既有 Turbopack trace warning）。
- PRD 功能矩阵改写为「功能规格库」+ mediaAssets URL 字段核实（文档变更，无代码改动）：用户反馈第一版 PRD 功能矩阵太总结式，要求每个功能写得极其细节；并要求核实 mediaAssets 是否缺 URL 字段。本轮：
  · mediaAssets 核实结论：URL 字段在代码里是有的（`normalizeMediaAssetRecord` spread 透传 `url` + `candidateUrls[]`，抖音 commentMedia.js:116 写 `url: candidateUrls[0]`），不是功能缺口，是 PRD 描述不全。已在 5.3 补上 `url / candidateUrls[]` 并加专门说明。
  · PRD 重构：第 1.3 节功能矩阵改为「功能索引表」（17 个功能，每个带 `#锚点` 跳转 + 状态徽章 ✅/🟡）；新增第 2 节「功能规格库」，每个功能（X1-X6 小红书 6 个 / D1-D8 抖音 8 个 / W1-W3 工作台 3 个）按统一模板展开：状态 / 入口 / 前置条件 / 输入参数（含真实默认值如评论上限默认 20、数量 [5,10,20,50]、maxPerSession=50）/ 数据源 / 写入字段 / 业务规则（含 D7/D8/D9/D18/D19/D23 等决策约束）/ 反检测参数（BATCH_CONFIG 全部数值：1200-2800ms 笔记间停顿、100-300px 滚动步长、15000ms 详情超时等）/ 状态机 / 异常处理（8 类错误映射）/ 验收点。
  · 容器型入口（Popup/页内控制台/Dashboard/Cookie&账号）单列 2.0 节，与采集功能规格分开。
  · 修正项：W2 自动接单规格补全 chrome.alarms 驱动细节（task poll alarm 30s = periodInMinutes 0.5、工位心跳 1min、collectionRun 心跳 30s 去 3s 抖、lease 2h、outbox 1s→2s→5s→15s→60s、in_flight 5min 复位、Web Push 唤醒 D21）；把上一轮反馈核查中确认的真实实现（chrome.alarms 驱动 outbox flush、IndexedDB 时间戳重试、accounts 含 cookieJson 字段）写进规格，避免再次被误读。
  · 保留并整合原有 mermaid 图：评论树层级（X3 内）、远程任务执行流程（W2 内）、数据层 ER、插件架构、工作台拓扑、主数据流。
  · 文档分层说明写在开头：第 2 节是 agent 实现 spec 权威，第 5-6 节是概览，完整字段/消息走 DATA_MODEL.md 和 MESSAGE_PROTOCOL.md。
  验证：纯文档改动，无构建/测试影响。本次同步文件：docs/product/PRD.md（主）、progress.txt（本条）。
- PRD 基于本地数据库真实结构补全 + 补流程图架构图完成（文档变更，无代码改动）：用户要求按本地数据库把 `docs/product/PRD.md` 功能细节补到位，并补流程图、架构图。对照 `src/db/index.js` v13 真实 schema 核对，原 PRD 第 5 节只列了 5 张表且字段几乎为空，遗漏了 `workbenchOutbox`（工作台增量发件箱，幂等键唯一索引 + 复合索引）和 `accounts`（采集账号，Cookie 轮换）两表，也无索引、去重、迁移历史、`chrome.storage.local` 扩展存储等内容。本次改动：
  · 第 1 节功能矩阵日期 2026-03-27 → 2026-06-25，补"detail_probe 目标解析已完成"行，抖音下载链路按 D18 标注页面上下文 fetch。
  · 第 2.2 评论采集补评论树层级 mermaid 图（rootCommentId/parentCommentId/level/replyToCommentId 关系）。
  · 第 2.4 批量内容采集补任务生命周期 stateDiagram（idle→running⇄paused→stopping→done/stopped/failed，含断点续跑说明）。
  · 第 2.7 工作台协同补远程任务执行流程 mermaid 图（reconcile→claim→capabilityCheck→dispatch→collectionRun→deltaOutbox→ingest→patchCollectionTask→自开页自动关闭）。
  · 第 5 节"数据存储"重写为"数据模型"：5.1 概览（Dexie v4/v13/6 表 + 扩展存储）、5.2 七表总览（主键/角色/关键索引，含 workbenchOutbox 和 accounts）、5.3 全字段明细、5.4 去重约束、5.5 v1→v13 迁移历史、5.6 chrome.storage.local（platformCookies/授权/工位/任务控制台）、5.7 数据层 ER 图。
  · 新增第 6 节"系统架构与数据流"：6.1 插件上下文架构图（页面主世界 Injected / 隔离世界 Content / Background·Popup·Dashboard / IndexedDB·storage / 工作台，含关键边界说明）、6.2 与工作台拓扑图（7 个 API 端点 × 5 个运行时模块）、6.3 主采集数据流图（触发→上下文→采集→normalize→入库→媒体→同步）、6.4 进度阶段 + 8 类错误映射、6.5 9 种远程任务类型 + 监控路由。
  · 末尾加附录"文档同步约定"，列出 PRD 联动的 7 份文档。
  原文措辞与第 1-4 节结构保留，未做激进重排。验证：纯文档改动，无构建/测试影响。本次同步文件：docs/product/PRD.md（主）、progress.txt（本条）。
- detail_probe 链路真实数据调研 + 过时测试更新完成（功能代码未改，符合"生产数据校准原则"）：上条回滚后，用新增的两个 probe 脚本在真实小红书页面跑了一次性调研：`scripts/probe-detail-probe-relay.js`（作者主页 + 详情页各一次）+ `scripts/probe-note-stored-urls.js`（本地库）。调研结论翻转了之前的判断——当前 v2.0.52 的 `resolvePreferredTaskTarget` 三层兜底逻辑其实是对的：(1) target 带 xsec_token → 直开（probe 验证：作者页卡片 href 点开后小红书自动路由成 `/explore/{noteId}?xsec_token=...` 带签名详情页，noteDetailMap 就绪、无风控）；(2) target 不带 token 但本地 getById 命中 → 用本地带 token URL（probe 验证：本地库 1310/1310 条都有某字段带 token，canonicalUrl 大量存了带 token 的 profile relay 形态 `/user/profile/{authorId}/{noteId}?xsec_token=...`，rawUrl 反而常空）；(3) target 不带 token 且本地无记录 → explore 兜底（probe 验证这是真正无解场景：作者主页 noteDetailMap 为空 → detail 模式超时；不带 token 的作者主页根本也打不开）。pre-existing 测试 `opens canonical author profile...` fail 是因为它测了第 (3) 类双重退化场景却期望"回到作者主页根"，而该期望来自 v2.0.47 前的 profile 模式接力模型，v2.0.47 改 detail 模型后已不成立——probe 实测作者主页根方案救不了。本次改动：`tests/background-task-target-resolution.test.mjs:82` 改成覆盖 probe 验证的真实有效路径——target 不带 token + 本地有带 token canonicalUrl（rawUrl 空，匹配生产数据分布）→ 返回本地带 token URL；功能代码 src/background/index.js 未改。新增 probe 脚本注册了 npm 命令 `probe:detail-probe-relay` / `probe:note-stored-urls`。验证：`node --test tests/background-task-target-resolution.test.mjs` 8/8 通过；`node --test tests/*.test.mjs` 530/530 全过（pre-existing fail 归零）；`npm run check:contracts` 通过；`npm run build` 通过，仅保留既有体积 warning。下一步（工作台侧，非本仓库）：确认 detail_probe 任务下发的 target URL 保留 xsec_token（从作者页卡片 href 来，正常应带），如丢失 token 则需在工作台侧修，插件侧无需再动。
- 本地工作台联调规则沉淀完成（文档约束，无代码改动）：内容工作台已切成本地数据库和本地 Blob 沙盒，插件本地打磨时必须选择内置“本地 3000”入口 `http://localhost:3000`；如果仍指向 `https://lingganboom.fun`，采集结果、任务回传、媒体上传和工位同步会进入线上正式站。已同步 `AGENTS.md`、`CLAUDE.md` 和 `docs/decisions/index.md`，规则要求切到本地后重新生成授权码、重新绑定执行设备，并先测试连接再采集。
- 撤销上一条 detail_probe URL 规范化修复（回滚 `src/background/index.js` 到 HEAD）：上一条修复让 `resolvePreferredTaskTarget` 把无签名 profile relay URL 规范成作者主页根，但 `/code-review` 追踪下游执行链发现这会引入新的 P0 bug——派单 mode 仍为 detail（taskEnvelopeMapper inferModeFromTarget 读 payload.targetPageType='detail'），`createXhsBatchStartHandler` `mode = msg.mode || 'search'` 拿到 'detail'，`BatchNoteController.start('detail')` 进入 `_captureCurrentDetailTask`，该路径假设当前页是详情页、等待 `__INITIAL_STATE__.note.noteDetailMap[noteId]` 就绪，但插件实际开的是作者主页根（不加载单篇详情数据），10s 超时后抛 "目标作品详情页未加载完成"。根因：v2.0.47（2594ff3）配套删除了 URL 规范化 + pageType 降级（`inferDispatchPageTypeFromTask`）两件事，pre-v2.0.47 能工作是因为 URL=profile根 + mode=profile（profile 模式走 filterTargetedXhsNoteList 锁定）；上一条修复只恢复了 URL 规范化、没恢复 pageType 降级，造成 URL(profile) 与 mode(detail) 不匹配。测试 `opens canonical author profile...` 转绿是假象——它只断言返回 URL，不覆盖下游执行链。回滚方式：`git checkout HEAD -- src/background/index.js`（该文件本轮唯一改动就是 detail_probe 修复，死代码清理未触及它）。回滚后状态：detail_probe 链路回到 v2.0.47 后的原状（无签名 profile relay → 规范成裸 explore/{noteId} → 30017 风控风险），这是 pre-existing 问题、非本次引入；该测试重新 fail（527/528）。下一步必须做带真实页面验证的完整调研后再修，不再凭代码印象改：要么完整恢复接力模型（URL 规范化 + pageType 降级一起恢复），要么保持 detail 模型但确保打开的是带 xsec_token 签名的可访问详情页（优先用本地 rawUrl）。
- 死代码清理 + 滞后文档修复完成（执行 2026-06-25 审查报告建议 1、2，无功能改动）：
  · 删除死代码：整文件 1 个（`src/content/commentCollectTask.js`，零代码引用，评论任务控制实际走 `commentTaskController.js` + `shared/managedTaskController.js`）；零引用导出符号 16 个——`shared/taskUi.js` 4 个 React 迁移遗留（applyTaskbarShellStyle/buildTaskbarMarkup/updateTaskbarView/applyFloatingToastStyle）+ 其连带未使用 import（acTaskbarShellStyle/acToastStyle/AC_TOKENS）、`themes/ac-ui/tokens.js` 3 个（dialogStyle/inputStyle/cardStyle）+ 连带未使用 const（DIALOG_GRADIENTS）、`shared/messaging.js` 的 sendToContent、`sync/flywheelSync.js` 的 checkFlywheelConnection、`themes/themeManager.js` 的 onThemeChange/isAcUiTheme、`platforms/xhs/batchShared.js` 的 parseBatchLikes、`workbench/runtime/monitorTask.js` 的 normalizeMonitorTaskStrategy、`content/protocol/responseEnvelope.js` 的 isContentEnvelopeAction、`workbench/protocol/schema.js` 的 isSupportedRemoteTaskType。
  · 修滞后文档：`docs/technical/TECH_STACK.md`（D1 schema v7→v13、D2 §4 删 chrome.debugger API + §5 权限清单删 debugger 补全 tabs/cookies/alarms/notifications、D3 host_permissions 对齐 manifest 真实值含 lingganboom.fun、D9 构建快照体积更新到 2026-06-25 实测）；`docs/ARCHITECTURE.md`（D4 版本 2.0.0→2.0.52、D5 测试数 32→110 两处、D6 §3.3 权限清单删 debugger 补全并加"不使用 debugger"说明、D7 §6 删 commentCollectTask.js 死文件行、D8 §10 messaging.js 工具描述 sendToContent→sendToTab、§3.2 通信图 sendToContent→sendToTab）。
  · 验证：`npm run check:contracts` 通过；`npm run build` 通过（仅保留既有体积 warning）；`node --test tests/*.test.mjs` 528 个用例 527 过 1 失败——唯一失败 `tests/background-task-target-resolution.test.mjs` 的 `resolvePreferredTaskTarget opens canonical author profile` 经 `git stash` 验证为 pre-existing 失败（原始代码同样失败），与本次清理无关，留待单独定位。
  · 体积变化：`content.js` 667→651 KiB、`background.js` 312→305 KiB（死代码清理的正面效果）。
- 全量审查 + 复刻蓝图交付完成（无代码改动，仅文档产出）：用户要求全面审查项目（架构/设计/可靠性/死代码/功能问题）并用代码事实写一份可被另一 agent 1:1 复刻的 PRD 手册。基于 v2.0.52 真实源码全量核对，产出两份文档：
  1. `docs/reviews/REVIEW_2026-06-25_FULL_AUDIT.md` — 审查报告。总体结论：项目完成度达企业级执行端雏形，代码/测试/文档三条线扎实。核心问题不是"写得烂"而是：(a) 导航文档滞后——TECH_STACK.md 写 Dexie v7 实际 v13、写有 debugger 权限实际无、host_permissions 写的不一致；ARCHITECTURE.md 写版本 2.0.0/32 测试实际 2.0.52/110 测试，且把死文件 commentCollectTask.js 列为活跃模块；(b) React 迁移后未清理的死代码——1 个整文件（src/content/commentCollectTask.js 零引用）+ 16 个零引用导出符号（shared/taskUi.js 7 个、sendToContent、checkFlywheelConnection 等）。
  2. `docs/REPLICATION_BLUEPRINT.md` — 自包含复刻蓝图（约 1252 行）。含：复刻铁律 R1-R5（防幻觉，明确"事实源=代码"、外部依赖必须先验证再写）；manifest.json 全文；MSG 全量 41+ 常量；Dexie v13 schema 全表；workbench 协议常量全表（任务类型/监控策略/事件类型/错误码/delta envelope/HTTP 端点）；13 个模块职责地图（含死文件标注）；外部依赖清单（XHS/抖音选择器+API+注入脚本通信协议，全部带验证日期）；magic number 全表；13 步复刻工作流建议；验收清单。蓝图明确标注 TECH_STACK.md 和 ARCHITECTURE.md 已滞后，复刻时以蓝图为准。
  本轮无代码改动，未触发构建/发布。下一步建议：先修滞后文档（D1-D9）+ 删死代码，再做复刻。

2026-06-24
- 小红书深度建档评论补采终态收口修复完成，版本提升到 v2.0.52：线上补采任务已采到目标作品和 10 条评论，工作台也收到过程记录，但任务长期停在 95% running，没有进入 completed。复核确认根因是小红书批量控制器在采集成功后先做页面收尾，再把本地运行标记为 done；如果页面收尾卡住，工作台只能持续收到心跳和过程进度，却等不到最终完成事件。本轮把小红书博主页扫描、批量笔记和详情评论补采三条成功路径改为“先确认运行完成，再做页面收尾”，保证结果已到手后任务能及时关单。发布包 `releases/linggan-boom-v2.0.52.zip` 已生成并同步内容工作台托管下载位，SHA256 为 `cf2b79f33d255f2427a0630a119b4ca8c9fa1888de1380b7740fd9defad366cc`，大小 `412540` 字节。验证：`node --test tests/workbench-xhs-batch-run-helper.test.mjs tests/xhs-batch-remote-startup.test.mjs tests/workbench-task-poller.test.mjs`、`npm run check:contracts`、`npm run build`、`npm run release:verify -- --version 2.0.52 --zip releases/linggan-boom-v2.0.52.zip` 通过；构建仅保留既有包体积 warning。
- 工作台任务页自动关闭修复完成并发布 v2.0.51：复核发现插件虽然已有“任务结束后关闭采集页”的设计，但终态清理主要依赖按任务编号回忆曾经打开的页；一旦后台恢复、任务对象被重建或终态清理只拿到本轮执行信息，插件自己为任务打开的页面可能没有被带到关闭队列，导致采集完成后页面残留。本轮把“插件自己打开的任务页”作为独立归属信息贯穿派单、活跃任务保存、恢复和终态清理：插件新开的任务页会随任务一起保存 `pluginOpenedTabId`，完成 / 失败 / 停止时即使任务编号回忆失败，也会按这个明确页签关闭；用户原本打开、只是被复用的页面不会被误关。发布包 `releases/linggan-boom-v2.0.51.zip` 已生成并同步内容工作台托管下载位，SHA256 为 `c88009675c71703c0e69446e3a6786e8f2558a82fd93282ea1786696bcef3ad3`，大小 `412521` 字节。验证：`node --test tests/workbench-task-execution-cleanup.test.mjs tests/workbench-task-poller.test.mjs`、`npm run check:contracts`、`node --test tests/workbench-task-execution-cleanup.test.mjs tests/workbench-task-poller.test.mjs tests/navigation-orchestrator.test.mjs`、`npm run build`、`npm run release:verify -- --version 2.0.51 --zip releases/linggan-boom-v2.0.51.zip` 通过；构建仅保留既有包体积 warning。

2026-06-23
- 小红书详情采集终态硬闭环完成，版本提升到 v2.0.50：指定程烈 2 运行 v2.0.49 实测确认，插件已经真实采回当前作品正文和 20 条评论，但工作台任务仍停在 95% running，随后被调度按“只有心跳无新数据”释放重排。根因是内容页发出的 `status=done` 进度事件和后台最终结果包之间仍可能出现终态错位；同时小红书批量笔记/批量评论控制器把 `collectionRunStore.markDone()` 的失败吞掉后继续对外报完成。本轮将小红书终态收口改成硬确认：笔记/评论控制器必须先把本地运行记录写成 done/stopped，失败则中断，不再发完成；配合工作台侧把带终态的进度事件纳入正式关单生命周期，并把小红书笔记任务最低接单版本提高到 2.0.50。发布包 `releases/linggan-boom-v2.0.50.zip` 已生成，SHA256 为 `ec539262924c4fa4d35b5a63017c0602d0a1a961d82c85a43fce39c80fb0b6d9`，大小 `412380` 字节。验证：`node --test tests/tech-debt-refactors.test.mjs tests/workbench-task-poller.test.mjs tests/workbench-result-packager.test.mjs`、`npm run check:contracts`、`npm run build` 和 release 包校验通过，构建仅保留既有包体积 warning。
- 小红书详情任务采完后正式收口修复完成，版本提升到 v2.0.49：指定程烈 2 运行 v2.0.48 实测确认，插件已经能一次采回当前作品正文、公开评论数和 20 条以内评论正文，样本「为什么我们连洗澡都要拖延」公开评论数 31、已回传评论正文 18 条；但内容页发出采集完成消息后，后台只清理角标，没有立刻唤醒工作台结果打包与任务完成上报，导致工作台侧可能长时间停在 running。本轮让工作台任务对应的采集完成消息直接触发一次强制任务收口检查，采完即进入结果交接，不再只等下一轮定时轮询。验证：`node --test tests/tech-debt-refactors.test.mjs tests/workbench-task-poller.test.mjs tests/workbench-result-packager.test.mjs`、`npm run check:contracts`、`npm run build` 通过。
- 小红书详情模式常量缺失根修完成，版本提升到 v2.0.48：指定程烈 1 / 程烈 2 升级到 v2.0.47 后重新派发两条真实详情任务，任务已由对应工位领取并打开采集页，但内容页控制器仍报“目标作品未在当前作者页找到”。复核发现 v2.0.46 / v2.0.47 虽然已把工作台任务和后台派发都传成 `mode=detail`，但共享 `COLLECT_MODE` 模式表没有注册 `DETAIL: 'detail'`，导致内容页判断 `mode === COLLECT_MODE.DETAIL` 永远无法匹配真实线上字符串，继续落回博主页列表扫描路径。本轮正式注册 `detail` 模式，并把回归测试改为使用线上真实字符串 `detail`，同时断言模式表必须存在该值。
- 小红书 profile 形式详情链接派发口径根修完成，版本提升到 v2.0.47：线上指定程烈 1 / 程烈 2 运行 v2.0.46 实测后发现，工位已升级且账号健康，但 `xhs.batchNotes + detail_probe` 任务仍在启动阶段报“目标作品未在当前作者页找到”，随后因只有心跳无数据被释放重排队。根因是插件后台为了兼容 `/user/profile/{authorId}/{noteId}` 形式链接，把未签名 profile relay 详情任务的 dispatch pageType 降级为 `profile`，导致内容页控制器仍以博主页列表模式启动，绕过了 v2.0.46 新增的当前详情页采集路径。本轮删除该旧派发口径：只要任务声明 `targetPageType=detail`，profile relay URL 也必须保持 detail 模式进入批量笔记控制器。验证：`node --test tests/background-page-type.test.mjs tests/monitor-detail-probe.test.mjs tests/workbench-capability-check.test.mjs tests/workbench-xhs-batch-run-helper.test.mjs tests/xhs-batch-remote-startup.test.mjs tests/navigation-orchestrator.test.mjs` 通过 68 个用例。
- 小红书详情任务当前页执行路径根修完成，版本提升到 v2.0.46：复核线上今天的监控详情任务失败后确认，工作台任务单和插件内容页启动层已经能传递 `includeComments / commentLimit=20 / targetNoteId`，评论采集与公开评论数判断能力也存在；真正断点在 `BatchNoteController` 接到 `detail` 模式后仍复用列表批量路径，先扫描当前页面卡片再点击打开，导致详情页任务出现“目标作品未在当前作者页找到”或只心跳无结果。现在 `detail` 模式直接采当前作品详情，再按同一条任务顺手采集 20 条以内评论；搜索页/博主页仍保留列表批量路径。补充回归测试覆盖 detail 模式进入当前详情采集路径、作者页带作品 ID 的详情链接识别、评论 0 与评论不足 20 的结果口径。验证：`node --test tests/workbench-xhs-batch-run-helper.test.mjs tests/xhs-batch-remote-startup.test.mjs tests/workbench-capability-check.test.mjs tests/navigation-orchestrator.test.mjs tests/monitor-detail-probe.test.mjs tests/platform-adapter-registry.test.mjs tests/background-page-type.test.mjs` 通过 73 个用例；`npm run build` 通过，构建仅保留既有包体积 warning。

2026-06-22
- 小红书详情任务评论参数透传根修完成，版本提升到 v2.0.45：code review 发现 v2.0.44 虽然把工作台远程 `xhs.batchNotes + detail` 从旧单篇入口切到了批量笔记入口，但内容页启动批量控制器时没有继续透传 `includeComments / commentLimit / commentDepthMode / targetNoteId`，导致控制器仍可能按“不采评论”执行。本轮在内容页启动层原样传递评论开关、20 条额度、评论深度和目标笔记 ID，并新增内容页级回归测试，证明远程详情任务到达执行控制器时字段完整。为避免同版本包混淆，版本提升到 `2.0.45`。发布包 `releases/linggan-boom-v2.0.45.zip` 已生成，SHA256 为 `dd9901d7c275e893f38456cee7296ec786993c4eabbb2348f834a1acb3e339b6`，大小 `411870` 字节。验证：`node --test tests/xhs-batch-remote-startup.test.mjs tests/background-page-type.test.mjs tests/workbench-capability-check.test.mjs tests/workbench-xhs-batch-run-helper.test.mjs tests/workbench-task-poller.test.mjs tests/xhs-comment-collector-state.test.mjs` 通过 103 个用例；`npm run check:contracts`、`npm run build`、`npm run release:verify -- --version 2.0.45 --zip releases/linggan-boom-v2.0.45.zip` 通过，构建仅保留既有包体积 warning。
- 小红书详情任务入口统一根修完成，版本保持 v2.0.44：线上今天的监控详情任务虽然由工作台下发了 `includeComments/commentLimit=20`，但完成回包仍是 `singleNote_*`，说明工作台远程 `xhs.batchNotes` 详情任务在插件后台被分流到只采正文的旧单篇入口，评论采集模块没有被调用。本轮移除工作台小红书详情任务到 `COLLECT_SINGLE_NOTE` 的分流，所有远程 `xhs.batchNotes + detail` 统一进入批量笔记控制器，由同一条路径负责正文、公开评论数和 20 条以内评论。托管下载位同步更新为 `2.0.44`，ZIP SHA256 为 `8b952d6f1592430bf9935068b5ae0dcf60ab5b675686777dd6718751b98fa085`，大小 `411816` 字节。验证：`node --test tests/background-page-type.test.mjs tests/workbench-capability-check.test.mjs tests/workbench-xhs-batch-run-helper.test.mjs tests/workbench-task-poller.test.mjs tests/xhs-comment-collector-state.test.mjs` 通过 98 个用例；`npm run build`、`npm run release:verify -- --version 2.0.44 --zip releases/linggan-boom-v2.0.44.zip` 通过。
- 工位同步信箱兜底修复完成，版本提升到 v2.0.43：插件任务轮询新增 10 分钟低频完整同步保险，即使某次 Station Mailbox 没有被正确更新，也会定期强制进入完整 `/sync` 检查，避免任务长期卡在队列里无人领取。内容工作台侧同步修复任务重新排队、过期预约释放后不更新工位信箱的问题。发布包 `releases/linggan-boom-v2.0.43.zip` 已生成，SHA256 为 `4b7b60fad481787cd749c5a10b39278f51381451fbdd77958aaae0eed3696e5b`，大小 `411698` 字节，并已同步到内容工作台 `plugin-releases/linggan-boom-latest.zip`。验证：`npm run build`、`npm run release:verify -- --version 2.0.43 --zip releases/linggan-boom-v2.0.43.zip` 通过，构建仅保留既有包体积 warning。
- 工作台工位同步协议切到 `/sync`，版本提升到 v2.0.42：插件心跳、对账和接单全部改为 `POST /api/execution-stations/sync`；状态型同步带 `claimMode=status_only`，只更新在线状态和信箱版本，不顺手接单；任务轮询同步才在需要时领取任务。旧 `/api/execution-stations/heartbeat`、`/api/execution-stations/reconcile`、`/api/execution-stations/dispatch` 和 `/api/collection-tasks/claim` 不再作为当前插件协议。发布包 `releases/linggan-boom-v2.0.42.zip` 已生成，SHA256 为 `6a7d936fe249d3739cffad6fbc91a420ff478ef340dd22ddc1609289a5e3395b`，大小 `411685` 字节，并已同步到内容工作台 `plugin-releases/linggan-boom-latest.zip`。验证：`node --test tests/workbench-task-lease.test.mjs tests/workbench-task-poller.test.mjs tests/workbench-control-sync.test.mjs tests/execution-station-client.test.mjs tests/workbench-task-poll-schedule.test.mjs` 通过 83 个用例；`npm run build`、`npm run release:verify -- --version 2.0.42 --zip releases/linggan-boom-v2.0.42.zip` 通过，构建仅保留既有包体积 warning。
- 小红书详情任务附带评论采集闭环完成，版本提升到 v2.0.41：内容工作台下发 `xhs.batchNotes` 且要求 `includeComments/commentLimit=20` 时，插件不再把详情任务降级到只采正文的单篇路径，而是继续走批量笔记控制器，在采完当前笔记正文、指标、公开评论数后顺手采集当前作品 20 条以内评论；公开评论数不足 20 条时按实际数量判断，公开评论数明确为 0 时记录为真实 0，不再误判为评论没采到。插件结果摘要新增附带评论结果里的 `publicCommentCount / expectedCommentCount / error` 语义，能区分“评论区确实为 0”和“应该有评论但没有正文返回”。同时修复评论区尚未渲染但页面接口已有评论快照时提前失败的问题。为避免用户已安装的未正式托管 `2.0.40` 与本轮发布混淆，最终可见版本提升到 `2.0.41`。发布包 `releases/linggan-boom-v2.0.41.zip` 已生成，SHA256 为 `80944890fc93c644979eba04b545fd81355baae8085f0b44e78d95c094a19806`，大小 `411339` 字节。验证：`node --test tests/background-page-type.test.mjs tests/monitor-author-surface-scan.test.mjs tests/workbench-xhs-batch-run-helper.test.mjs tests/xhs-comment-collector-state.test.mjs` 通过 40 个用例；`npm run build`、`npm run release:verify -- --version 2.0.41 --zip releases/linggan-boom-v2.0.41.zip` 通过，构建仅保留既有包体积 warning。

2026-06-17
- 工作台所有心跳入口统一唤醒拉单，版本提升到 v2.0.39：继续线上复查发现，部分工位已经显示 v2.0.38 并持续心跳，但最近一次接单时间仍停留在数小时前，说明问题不只是一轮拉单 Promise 卡死；插件里还存在多条“只上报心跳、不根据服务端 shouldPollNow 继续拉单”的入口，例如插件页面主动刷新心跳、授权/注册后的心跳、服务 worker 启动后的心跳。这个设计会让监控中心看到工位在线、版本已更新，但任务仍没有被实际领取。本轮将心跳结果处理收敛成统一路径：任何成功心跳只要服务端提示有到期待接任务，都会立即触发工作台任务接单检查；服务 worker 启动后也会在安装引导完成后主动跑一次心跳与接单检查。该改动不改变任务协议、租约、页面采集和结果回传。发布包 `releases/linggan-boom-v2.0.39.zip` 已生成，SHA256 为 `12c09c9629a9310ae1f79bdf769e970d3cf73bb5469539b1806f9b6662ccdfae`，大小 `410561` 字节。验证：`node --test tests/workbench-task-poll-schedule.test.mjs tests/workbench-task-poller.test.mjs` 通过 57 个用例；`npm run check:contracts`、`npm run build`、`npm run release:verify -- --version 2.0.39 --zip releases/linggan-boom-v2.0.39.zip` 通过，构建仅保留既有包体积 warning。
- 工作台拉单循环卡死自恢复完成，版本提升到 v2.0.38：线上排查发现部分执行工位已升级并持续心跳，但最近一次接单时间仍停留在数小时前，说明“心跳唤醒”之外，插件本地拉单器还存在单次 tick 卡住后长期阻塞后续接单的风险。本轮为工作台任务拉单器增加总超时保护：如果上一轮拉单/派单循环超过保护时间仍未结束，后续心跳或定时轮询会允许发起新的拉单检查，不再被旧的未完成 Promise 永久挡住。该改动不改变任务租约、能力检查、页面采集和结果回传协议。发布包 `releases/linggan-boom-v2.0.38.zip` 已生成，SHA256 为 `0a56ce1ef69746088bc74c06f47abd76ea8e3e3c8d00dd524f19f0933c1396a5`，大小 `410487` 字节。验证：`node --test tests/workbench-task-poller.test.mjs` 通过 47 个用例；`node --test tests/workbench-task-poller.test.mjs tests/workbench-task-poll-schedule.test.mjs tests/workbench-task-lease.test.mjs tests/workbench-control-sync.test.mjs` 通过 77 个用例；`npm run check:contracts`、`npm run build`、`npm run release:verify -- --version 2.0.38 --zip releases/linggan-boom-v2.0.38.zip` 通过，构建仅保留既有包体积 warning。
- 工作台待领取任务心跳唤醒修复完成，版本提升到 v2.0.37：内容工作台新增 `shouldPollNow` 心跳信号，当后台发现有到期待接任务时，会要求插件不要继续等待本地空闲排程；插件端收到该信号后立即执行一次工作台任务接单检查，避免出现“工位心跳在线，但几个小时没有发起接单”的卡死。该改动只改变工作台任务轮询触发，不改变页面采集、租约、能力检查、结果回传和控制同步路径。验证：`node --test tests/workbench-task-poll-schedule.test.mjs` 通过 9 个用例；发布包 `releases/linggan-boom-v2.0.37.zip` 已生成并同步到内容工作台托管下载位。

2026-06-16
- 工作台统一接单入口适配完成，版本提升到 v2.0.35：内容工作台已废弃旧 `/api/collection-tasks/claim` 接单入口，只保留 `/api/execution-stations/dispatch` 统一处理工位心跳、对账与接单。插件接单客户端已改为请求 dispatch，并把 `heartbeat / reconcile / claim` 返回结构归一成原有任务执行器可识别的任务租约格式；接单请求同步上报 `pluginVersion` 和本地租约，避免新版服务端版本校验或租约恢复失败。发布包 `releases/linggan-boom-v2.0.35.zip` 已生成，SHA256 为 `d5dc590915620c3e0135a19ac4a29595249e64945642f3a4dea94c618d4b467e`，大小 `410378` 字节；包内检查确认不再包含 `/api/collection-tasks/claim`，只包含 `/api/execution-stations/dispatch`。验证：`node --test tests/workbench-task-lease.test.mjs tests/workbench-task-poller.test.mjs tests/workbench-control-sync.test.mjs tests/execution-station-client.test.mjs` 通过 72 个用例；`npm run test:douyin` 通过 216 个用例；`npm run check:contracts`、`npm run build`、`npm run release:verify -- --version 2.0.35 --zip releases/linggan-boom-v2.0.35.zip` 通过，构建仅保留既有包体积 warning。

2026-06-12
- 深度博主历史笔记链接发现能力接入：新增工作台远程任务 `xhs.authorNoteLinks`，插件会在小红书博主主页滚动发现历史笔记卡片，只回传作品链接、签名链接、作者身份、任务编号和发现顺序，不打开详情页。执行工位能力上报新增 `xhs.authorNoteLinks`，页面能力报告只在小红书博主主页声明可执行，避免关键词页或详情页误接。任务完成时会写回计划发现数、实际发现数、短缺数和每条 note 记录，工作台可据此判断哪些链接已回收、哪些后续详情补采失败后需要重采。验证：`node --test tests/content-collection-handlers.test.mjs tests/workbench-capability-check.test.mjs tests/navigation-orchestrator.test.mjs tests/background-page-type.test.mjs tests/execution-station-runtime.test.mjs` 通过 57 个用例；`npm run check:contracts` 通过；`npm run build` 通过，保留既有包体积 warning。
- 小红书数据治理评论回传热修完成，版本提升到 v2.0.34：线上数据治理补采发现评论任务会因为单条评论缺少正文被插件端结构校验判为整批失败，错误为 `comment payload must include comment text`。本轮把远程任务结果整理收紧为：评论必须同时具备评论 ID、所属笔记 ID 和正文才进入工作台回传队列；空正文或缺父级作品的评论会被丢弃，不再拖垮整条治理任务。笔记、作者、媒体等其它记录仍保留严格结构校验，坏笔记仍会触发 schema 健康告警。发布包 `releases/linggan-boom-v2.0.34.zip` 已生成，SHA256 为 `9b266811b4d2d1e7ca234fb063f854c61049cd2ab977b43de564373b6b25971f`，大小 `408444` 字节，并已同步到内容工作台 `plugin-releases/linggan-boom-latest.zip`。验证：`node --test tests/workbench-task-poller.test.mjs` 通过 46 个用例；`node --test tests/workbench-result-packager.test.mjs tests/workbench-task-lease.test.mjs` 通过 15 个用例；`npm run check:contracts`、`npm run build`、`npm run release:verify -- --version 2.0.34 --zip releases/linggan-boom-v2.0.34.zip` 通过，构建仅保留既有包体积 warning。

2026-06-06
- 数据地基回传字段接入完成，版本提升到 v2.0.33：插件新增 `dataFoundationPayload` 出站整理器，工作台任务增量回传和手动同步都会在笔记记录里附带 `standardContentCode`、`standardAuthorCode`、`contentType`、`keywords`、`authorFans`、`authorFansCollectedAt`、`mediaUnderstanding`、`sourceRun` 和 `dataFoundation` 摘要。作者粉丝数同时支持 `1.2万`、`3w`、`8千`、`2.5k followers`、`1,234 粉丝` 等平台写法，避免低粉爆文筛选漏数。它只整理已有采集结果，不做业务判断；工作台后续可直接用这些字段做今日低粉爆文、爆款聚类、证据完整性和 Claude Agent 打标输入。发布包 `releases/linggan-boom-v2.0.33.zip` 已生成，SHA256 为 `52b52c806d983aeb531c5b0cca405c89fdf2f2d185ecf830b12f08328fa44da6`，大小 `408426` 字节。验证：node --test tests/data-foundation-payload.test.mjs tests/workbench-delta-outbox.test.mjs tests/workbench-task-poller.test.mjs tests/workbench-capability-check.test.mjs 通过 74 个用例；npm run check:contracts、npm run build、npm run release:verify -- --version 2.0.33 --zip releases/linggan-boom-v2.0.33.zip 通过。

2026-06-01
- 小红书搜索筛选等待的评审问题已修复：真实页面上计算样式为隐藏的 `.feeds-loading` 不再被误判为仍在加载；带筛选条件的远程搜索采集启动等待窗口放宽到覆盖筛选稳定等待，避免任务还在等小红书刷新时被上层 8 秒启动超时误判失败。验证：node --test tests/xhs-search-filters.test.mjs tests/xhs-batch-remote-startup.test.mjs tests/xhs-batch-record-delta.test.mjs 通过 16 个用例；npm run build 通过，保留既有 webpack bundle 体积 warning。
- 小红书搜索筛选后的列表稳定等待完成：筛选点击并确认选中后，批量笔记和批量评论会先显示“等待筛选结果加载”，读取 `.feeds-container` 前若干真实笔记摘要，等列表刷新且连续稳定后才开始扫描；隐藏的加载占位不会误判为页面仍在加载。如果页面迟迟不稳定，任务会停在筛选阶段并提示刷新后重试，避免网络延迟时采到筛选前的旧列表。已同步产品流程、验收清单、选择器清单、调研文档和技术决策日志，并在外部 Chrome 重新加载“灵感爆爆爆 2.0.28”后刷新当前小红书搜索页。验证：node --test tests/xhs-search-filters.test.mjs tests/xhs-batch-remote-startup.test.mjs tests/xhs-batch-record-delta.test.mjs 通过 15 个用例；npm run build 通过，保留既有 webpack bundle 体积 warning。
- 小红书搜索页筛选功能第一期落地：搜索页批量采集弹窗新增“排序依据、笔记类型、发布时间”三组筛选，支持综合/最新/最多点赞/最多评论/最多收藏、不限/视频/图文、不限/一天内/一周内/半年内；按本轮需求不实现“搜索范围”和“位置距离”。采集启动时会先应用所选筛选，再扫描笔记流；如果选择“沿用当前”，则不改动页面，只记录当前筛选快照。小红书搜索页旧的“按点赞 Top N”在搜索场景收起，改由页面自身“最多点赞”承接。已同步 `docs/SELECTORS.md` 和 `docs/technical/XHS_FIELD_SURVEY.md`。验证：node --test tests/xhs-search-filters.test.mjs、node --test tests/xhs-batch-remote-startup.test.mjs、node --test tests/xhs-batch-record-delta.test.mjs 通过 10 个用例；npm run build 通过，保留既有 webpack bundle 体积 warning。
- 外部 Google Chrome 实测发现小红书筛选面板存在重复按钮层，先点到外层时页面不会真正切换筛选，可能导致插件越过筛选直接进入采集。本轮已修复：筛选点击会优先命中真实生效按钮，并在每组筛选后确认页面已选中；如果页面没有切过去，任务直接报“小红书筛选失败”并收起控制台，不再继续打开笔记造成卡住。验证：node --test tests/xhs-search-filters.test.mjs tests/xhs-batch-remote-startup.test.mjs tests/xhs-batch-record-delta.test.mjs 通过 11 个用例；npm run build 通过，保留既有 webpack bundle 体积 warning。
- 小红书搜索页“笔记流容器”页面结构回归完成：基于已登录真实小红书页面验证关键词“课题分离”搜索结果，`.feeds-container` 仍存在且可见；首屏 30 个 `section.note-item` 中 27 个为真实笔记、3 个为“相关搜索”，因此采集仍必须以 `a.cover + noteId` 过滤真实笔记，不能只数卡片外壳。滚动 6 轮累计发现 57 个唯一笔记 ID；`window.__INITIAL_STATE__.search.feeds` 当前有 88 条，其中 80 条 note、8 条 rec_query，可作为后续列表发现兜底候选。打开首条搜索卡片后实际进入 `/explore/{noteId}`，`#noteContainer / .note-container`、`.comments-container` 与 `note.noteDetailMap[{noteId}]` 均命中。已同步 `docs/SELECTORS.md` 和 `docs/technical/XHS_FIELD_SURVEY.md`，并仅刷新搜索页笔记流选择器的代码验证日期，未把博主页等未复验信号一并刷新。验证：node --test tests/xhs-selector-health.test.mjs tests/selector-health-notice.test.mjs 通过 6 个用例；npm run build 通过，保留既有 webpack bundle 体积 warning。

2026-06-25
- 本地工作台授权与工位配对无响应修复完成，版本保持 v2.0.52：本地 3000 真实接口验证通过，审批申请 -> 工作台同意 -> 插件领取审批结果会自动生成工位，授权码激活 -> 配对码绑定也能完成工位注册。排查本地库发现有“已同意但未领取”的审批申请，以及多条“授权已激活但未绑定工位”的记录，符合先走审批、再用授权码反复尝试后插件本地状态被覆盖的现象。本轮修复插件领取审批结果的边界：只有工作台返回新的授权令牌时才视为真正领取成功；如果用户在已激活授权码后误点“检查审批结果”，插件会保留原授权令牌，不再把本地授权清空。Popup 发后台消息新增 15 秒超时提示，后台无响应时不再表现为按钮没反应。已重新构建 dist，刷新 `releases/linggan-boom-v2.0.52.zip`、`dist.zip`，并同步内容工作台 `plugin-releases/linggan-boom-latest.zip`。验证：node --test tests/plugin-authorization-client.test.mjs tests/popup-ui-copy.test.mjs 通过 17 个用例；npm run build 通过，保留既有 webpack bundle 体积 warning；npm run release:verify 通过；内容工作台插件下载测试 npx vitest run src/lib/services/plugin-release-service.test.ts src/app/api/plugin-release/latest/route.test.ts src/app/api/plugin-release/latest/download/route.test.ts 通过 19 个用例。

2026-05-29
- 抖音工作台账号旧占用与执行页定位丢失热修完成，版本保持 v2.0.26：线上任务 `cmpqfigba0033ky04u3vtwcak` 在 12:37 附近反复回写“同一账号正在执行另一个采集任务”，本地插件存储证据显示同一个抖音账号 `ac_mpjvy55u_aevo2mbx` 仍保留上一条已停止/删除任务 `cmpqe86f701avjr04tey8diuo` 的执行占用，导致新建档任务被旧占用挡住到超时自然释放；恢复接单后又因为同一本地执行轮次在续约时换了服务端尝试编号，执行页 tabId 被误清空，结果包回取找不到本轮页面。现在工作台任务接单时如果发现占用来自另一条旧工作台任务，且本机已经没有对应旧任务租约，会先释放旧占用再重新接当前任务；同一 `pluginRunId` 的续约会保留本轮执行页定位，避免采集已完成但结果包找不到页面。`manual:*` 手动同步占用和仍在执行的当前任务不会被误清。发布包 `releases/linggan-boom-v2.0.26.zip` 已重打，SHA256 为 `d72dfbeb6d587183e1108d936a39f80692658edfe06ba4eec9c5c437c7fa196a`，大小 `397525` 字节。验证：node --test tests/workbench-task-poller.test.mjs tests/execution-account-lock.test.mjs 通过 50 个用例；npm run check:contracts、npm run test:douyin 通过 205 个用例；npm run build 通过，保留既有 webpack bundle 体积 warning；npm run release:verify -- --version 2.0.26 --zip releases/linggan-boom-v2.0.26.zip 通过。
- 抖音工作台结果包交接丢失根修完成，版本提升到 v2.0.26：线上证据显示“程烈 1”接到的抖音博主建档任务在抖音页本地已经从 running 进入 done，并且 `resultUploadStatus` 已到 packaged，页面库里也有大量同一 `collectionRunId` 的作品记录；但工作台侧只有 heartbeat，没有 `task.record / task.completed`。根因不是抖音没有采到，而是工作台轮询器没有保留本次执行页 tabId，结果回取时可能落到后台本地空库，找不到页面侧结果包。现在派单成功会把执行页 tabId 带回并持久保存，结果包回取优先直连本轮执行页；如果 running 任务超过 12 分钟仍找不到结果包，会明确失败为“结果包没有交回工作台”，不再无限心跳假运行。Dashboard 手动同步小红书评论 50 / 200 / 500 条的“未知错误”同步修复：等待时间从默认 3 秒提升到 45 秒，评论同步成功提示改按工作台返回的评论登记数统计。追加修复监控博主任务的停止控制：作者建档类任务现在会接收工作台停止/删除指令，停止后不再继续采集，已经拿到的作者结果会按停止状态打包，避免页面还在跑但工作台一直没有写回。发布包 `releases/linggan-boom-v2.0.26.zip` 已生成并同步到工作台 `plugin-releases/linggan-boom-latest.zip`，SHA256 为 `5479517fce0f1aacf5f3d9b700791b487cb756766e42cff081caa4f0f595b345`，大小 `397241` 字节。验证：node --test tests/workbench-author-remote-run.test.mjs tests/content-workbench-handlers.test.mjs tests/workbench-task-poller.test.mjs 通过 51 个用例；node --test tests/background-task-target-resolution.test.mjs tests/dashboard-workbench-sync.test.mjs tests/workbench-task-poller.test.mjs tests/workbench-author-remote-run.test.mjs tests/content-workbench-handlers.test.mjs 通过 63 个用例；npm run check:contracts、npm run test:douyin、npm run build、npm run release:verify -- --version 2.0.26 --zip releases/linggan-boom-v2.0.26.zip 通过。
- 抖音工作台任务页不可读恢复修复完成：线上确认指定到“程烈 1”的抖音博主建档任务已经被该工位接单，但能力检查阶段遇到 Chrome 返回“Cannot access contents of the page. Extension manifest must request permission to access the respective host.”，导致任务没有进入采集即被判失败。本轮把这类 Chrome 页面不可读错误纳入任务页连接恢复范围：选择已打开任务页时会跳过不可读标签页继续找可用页，必要时走重新打开任务页的路径；能力检查和派发阶段也统一识别该错误，避免把临时不可读页误当成任务本身必然失败。验证：node --test tests/background-task-target-resolution.test.mjs tests/workbench-task-poller.test.mjs tests/shared-messaging-send-to-tab.test.mjs 通过 50 个用例；npm run test:douyin 通过 201 个用例；npm run check:contracts 通过；npm run build 通过，保留既有 webpack bundle 体积 warning。

2026-05-27
- 工作台派单的不可访问笔记处理修复完成，版本提升到 v2.0.24：小红书笔记已删除、页面错误或无权限访问时，能力检查会优先保留 `content_not_found / error_page / page_permission_denied` 这类真实原因，不再被误报为“当前页面能力报告未声明支持该任务类型”。插件接到这类明确不可执行的任务后会把任务标记为 failed 并写明原因，不再退回 pending 造成程烈 1 / 程烈 2 反复接单；`task.capability_mismatch` 事件同步带上页面报告摘要（页面 URL、页面类型、准备状态、可执行任务清单），便于线上排查。发布包 `releases/linggan-boom-v2.0.24.zip` 已生成并同步到工作台 `plugin-releases/linggan-boom-latest.zip`，SHA256 为 `9d6e6b1a79f234d37677e44e42c4cff4d65fbbda89fed415865701395e6f68d3`，大小 `396052` 字节。验证：node --test tests/workbench-capability-check.test.mjs tests/workbench-task-poller.test.mjs 通过 52 个用例；npm run test:douyin 通过 199 个用例；npm run check:contracts 通过；npm run build 通过，保留既有 webpack bundle 体积 warning；npm run release:verify -- --version 2.0.24 --zip releases/linggan-boom-v2.0.24.zip 通过。

2026-05-25
- Content 消息处理器职责拆分完成：原近千行 `src/content/messageHandlers.js` 缩成兼容门面，真实处理逻辑拆到 `messageHandlers/dataHandlers.js`、`collectionHandlers.js`、`workbenchHandlers.js`、`mediaHandlers.js`，并新增 `remoteControlRegistry.js` 统一管理远程任务暂停 / 继续 / 停止控制。`src/content/index.js` 不再直接截获无 `taskControl` 的工作台控制消息，旧式停止 / 暂停 / 继续也统一进入 runtime 工作台处理器，并补充真实消息入口回归测试。测试同步拆成数据、采集、媒体、工作台、监听入口和门面冒烟六组，架构文档、消息协议和决策日志已同步。验证：node --test tests/content-message-listener.test.mjs tests/content-message-handlers.test.mjs tests/content-data-handlers.test.mjs tests/content-collection-handlers.test.mjs tests/content-media-handlers.test.mjs tests/content-workbench-handlers.test.mjs tests/workbench-author-remote-run.test.mjs tests/workbench-douyin-remote-long-tail.test.mjs 通过 32 个用例；node --test tests/*.test.mjs 通过 450 个用例；npm run test:douyin 通过 193 个用例；npm run check:contracts 通过；npm run build 通过，保留既有 webpack bundle 体积 warning；同版本发布包 releases/linggan-boom-v2.0.21.zip 已重打并通过 release:verify，SHA256 为 c00117101b94b4bf27015b8bc72c82dc5b4bcb56c09f8fa59147476bb7575d9f，大小 394938 字节。

2026-05-24
- 执行可靠性补充收口完成，版本提升到 v2.0.19：批量任务新增断点续跑 checkpoint，XHS / Douyin 批量笔记与批量评论在页面刷新或后台恢复后从下一条继续；extractor 输出新增最小结构校验，坏记录不再进入同步队列并伪装成功；运行健康上报新增 schema 校验计数、失败字段和 reason code；页面能力判断改为统一 Platform Adapter；工作台能力不匹配事件已补进插件协议常量；协议边界新增 checkJs；发布包 `releases/linggan-boom-v2.0.19.zip` 已生成并同步工作台下载目录，SHA256 为 `3e1304635807e6d43e205a58591c5560b598d6d9c07db807a3da0d52a88e625b`，大小 `394011` 字节。验证：定向可靠性 / Adapter / schema / 故障演练测试通过；npm run check:contracts、npm run build、npm run release:verify -- --version 2.0.19 --zip releases/linggan-boom-v2.0.19.zip 通过。

2026-05-22
- 执行设备能力上报、失败分类与 v2.0.17 发布包收口完成：插件心跳、接单和租约对账会同时上报小红书 / 抖音页面权限、Cookie 可读性、登录态和平台拦截状态；如果浏览器没有平台页面权限、平台登录失效或触发拦截，服务端可以在派单前过滤掉这个设备，不再把任务派给跑不了的平台。远程任务错误码新增权限失败、页面错误、登录失效、平台拦截、内容不存在和只有心跳无数据分类，便于工作台按原因展示和重试。版本提升到 v2.0.17，本地发布包 `releases/linggan-boom-v2.0.17.zip` 已生成并通过 zip/dist 一致性校验。验证：node --test tests/execution-station-runtime.test.mjs tests/workbench-progress-error.test.mjs 通过 13 个用例；npm run test:douyin 通过 173 个用例；npm run build 通过，保留既有 webpack bundle 体积 warning；npm run release:verify -- --version 2.0.17 --zip releases/linggan-boom-v2.0.17.zip 通过。

2026-05-19
- 工作台 Web Push 主动唤醒 v3 一期完成，版本提升到 v2.0.15：插件在安装、启动、配对和心跳成功后，会向内容工作台注册 Chrome Web Push 订阅；工作台有新执行任务或任务控制变化时，会推送 `collection_task_available / collection_task_control` 轻量消息。插件收到推送后只把原有接单检查提前执行，不绕过 claim、lease、control、ingest 等安全链路；v2 低频轮询继续保留为兜底。验证：node --test tests/workbench-push-subscription.test.mjs tests/execution-station-client.test.mjs tests/workbench-task-poll-schedule.test.mjs tests/workbench-task-poller.test.mjs tests/workbench-task-lease.test.mjs tests/workbench-control-sync.test.mjs 通过 62 个用例；npm run test:douyin 通过 167 个用例；npm run build 通过，保留既有 webpack bundle 体积 warning；npm run release:verify -- --version 2.0.15 --zip releases/linggan-boom-v2.0.15.zip 通过。
- 工作台空闲接单降频与插件 v2.0.14 完成：内容工作台在无待处理任务时会让插件约 2 分钟后再检查，能力不匹配同样按约 2 分钟等待；账号需要重新登录、受限、达到日上限或不可用时按约 5 分钟等待。插件后台新增“心跳不绕过接单等待”保护：空闲心跳只维持工位在线，不会立刻触发 claim；已有活跃任务时仍继续短周期续约、控制同步和结果回写。验证：node --test tests/workbench-task-poll-schedule.test.mjs 通过 8 个用例；node --test tests/workbench-task-poller.test.mjs tests/workbench-task-lease.test.mjs tests/workbench-control-sync.test.mjs tests/execution-station-client.test.mjs 通过 47 个用例；npm run build 通过，保留既有 webpack bundle 体积 warning；npm run release:verify -- --version 2.0.14 --zip releases/linggan-boom-v2.0.14.zip 通过。

2026-05-18
- 抖音发布数据同步写回缺内容修复完成，版本提升到 v2.0.13：抖音远程单篇评论任务现在会把当前视频记录重新绑定到本次工作台任务的 `collectionRunId`，结果包能带回视频标题、正文、封面、点赞、收藏、评论数等主记录，不再只上传评论汇总导致工作台“任务完成但数据管理看不到内容写回”。这次只调整抖音远程任务的数据归属，不改变普通采集、媒体下载或任务调度协议。验证：node --test tests/workbench-result-packager.test.mjs tests/workbench-task-poller.test.mjs tests/workbench-douyin-remote-long-tail.test.mjs 通过 39 个用例；npm run test:douyin 通过 167 个用例；npm run build 通过，保留既有 webpack bundle 体积 warning。
- 小红书媒体下载失败修复与弹窗分项选择完成：对标 `iszhouhua/social-media-copilot` 后确认其核心做法是保留媒体类型选项并直接交给浏览器下载，本轮将小红书图片笔记从后台 ZIP 拉取改为浏览器逐个下载，失败时再走页面内 fetch 兜底，避免采集后出现“成功 0/N、失败 N”。采集后媒体弹窗支持封面、所有图片、Live、视频分项勾选；视频选项只在存在视频候选时出现。小红书采集新增 `livePhotoStreams`，并补齐 `h_265 / h_264 / master_url / backup_urls` 这类平台字段。Dashboard “媒体”按钮复用同一下载队列，可识别保存记录里的对象型图片候选并逐个下载。验证：node --test tests/note-media-download.test.mjs tests/xhs-video-stream-selection.test.mjs tests/xhs-note-collector.test.mjs tests/xhs-page-controller-summary.test.mjs 通过 22 个用例。

2026-05-15
- 二次测试反馈收口完成：链接已足够，已对标克隆 `/tmp/social-media-copilot` 核查竞品源码。小红书媒体弹窗改成可点击的分项选择卡片，支持封面、静态图、Live 动图、视频任意多选，并支持“只下此项”；小红书评论数优先读取页面笔记结构数据里的 `interactInfo.commentCount`，再兜底读可见文字，避免弹窗拿不到当前评论量；抖音图文从悬浮面板点“下载视频”时会重新识别图文内容并下载图片 ZIP，且图集图片拉取失败时会走与数据面板一致的后台拉取兜底。验证：node --test tests/note-media-download.test.mjs tests/xhs-comment-api.test.mjs tests/content-message-handlers.test.mjs tests/douyin-video-security.test.mjs tests/xhs-note-collector.test.mjs 通过 31 个用例；node --test tests/*.test.mjs 通过 325 个用例；npm run build 通过，保留既有 package type warning 和 webpack bundle 体积 warning。
- 测试反馈的采集能力补强完成，保持插件授权、工作台接单和同步架构不变：媒体下载弹窗从“下载全部”升级为可选封面、静态图、Live 动图、视频；小红书 Live 图按图片序号合并成一个 ZIP，避免 Chrome 弹出多条失败下载记录；抖音图文内容会按图片打包，不再把黑色视频当媒体下载；Dashboard 增加单独“封面”下载按钮。评论采集弹窗现在会在能读取到页面数据时显示当前评论数，并提供“采集子评论”开关，小红书、抖音、单篇、批量和工作台派单路径都同步尊重该选择。数据面板会动态显示新增媒体/文字/音乐字段，导出和手动同步也保留这些字段。验证：node --test tests/note-media-download.test.mjs tests/xhs-comment-api.test.mjs tests/content-message-handlers.test.mjs 通过 21 个用例；相关面板/远程任务/抖音回归 30 个用例通过；node --test tests/*.test.mjs 通过 325 个用例；npm run build 通过，保留既有 package type warning 和 webpack bundle 体积 warning。

2026-05-14
- 竞品采集能力对标后的第一批补强完成，保持现有插件授权、工作台接单、同步架构不变，只补全采集字段和媒体候选：小红书新增图片 trace/file 元数据、高质量图片候选和 live photo 视频流，live photo 会进入媒体下载队列并登记为 `live_photo` 资产；抖音新增图文媒体、封面候选、视频变体、OCR 画面文字、音乐链接、章节/合集信息和 admire 互动计数。工作台任务回传只在字段非空时携带这些增强数据，避免污染旧 payload。验证：node --test tests/xhs-note-collector.test.mjs tests/note-media-download.test.mjs tests/douyin-video-security.test.mjs 通过 16 个用例；node --test tests/flywheel-sync-quality.test.mjs tests/dashboard-workbench-sync.test.mjs tests/workbench-task-poller.test.mjs 通过 32 个用例；node --test tests/*.test.mjs 通过 321 个用例；npm run build 通过，保留既有 webpack bundle 体积 warning。

2026-05-13
- 数据面板“媒体”按钮下载失败修复完成：根因是部分小红书保存记录里的图片候选是 `{ url: ... }` 这类对象，媒体下载队列之前会把它误当成普通文本，导致后台拿不到真实图片地址并显示失败。修复后候选链接归一化会识别保存记录对象、数组和备用链接字段，数据面板下载能拿到真实媒体链接并继续打包 ZIP；Dashboard 验收清单已补小红书图片笔记二次下载项。验证：node --test tests/note-media-download.test.mjs 通过 5 个用例；node --test tests/note-media-download.test.mjs tests/dashboard-response-envelope.test.mjs tests/dashboard-bridge-sync.test.mjs 通过 11 个用例；node --test tests/*.test.mjs 通过 318 个用例；npm run build 通过，保留既有 package type warning 和 webpack bundle 体积 warning。

2026-05-12
- 工作台任务空轮询错峰修复完成，版本提升到 v2.0.9：普通空任务等待现在先遵守服务端 nextPollAfterMs，再增加 5-15 秒随机错峰，避免多个插件同时进入固定轮询节奏；明确的账号冷却、工作台限流、插件协议限流仍保持服务端给定的精确等待时间。验证：node --test tests/workbench-task-poll-schedule.test.mjs 通过 6 个用例；相关工作台任务测试 54 个用例通过；npm run build 通过，保留既有 webpack bundle 体积 warning。

2026-05-09
- 小红书视频笔记下载失败修复完成：根因是视频大文件走“后台拉取为 dataUrl → 发回内容脚本 → ZIP 打包”的路径，容易因大文件消息搬运失败；同时小红书旧视频直链过期后没有刷新重试，且 h266 / 备用视频链接没有完整进入候选队列。修复后图片笔记仍打包 ZIP，视频笔记直接下载视频文件；失败时会刷新当前笔记媒体链接后重试；视频候选流补齐 h266、backupUrls、urlList 等备用来源。验证：node --test tests/note-media-download.test.mjs tests/xhs-video-stream-selection.test.mjs 通过 5 个用例；相关回归 18 个用例通过；node --test tests/*.test.mjs 通过 298 个用例；npm run build 通过，保留既有 webpack bundle 体积 warning。
- Dashboard 首次打开后误判空数据修复完成：根因是数据面板 iframe 首次加载时只从 storage 读取本次打开的通行码，storage 尚未同步时会发空通行码，外层页面拒收后等待超时，表现为转圈数秒再显示“还没有采集数据”。修复后打开 Dashboard 时会把通行码同时放入 iframe URL，面板优先读 URL 通行码，并等待通行码落库后再完成打开响应。验证：node --test tests/dashboard-response-envelope.test.mjs tests/dashboard-bridge-sync.test.mjs tests/workbench-response-envelope.test.mjs 通过 10 个用例；node --test tests/*.test.mjs 通过 296 个用例；npm run build 通过，保留既有 webpack bundle 体积 warning。

2026-05-06
- 普通同步使用者归属修复完成：插件授权只作为使用资格，不再直接决定数据写入账号；同步前会向内容工作台刷新“当前使用者数据身份”，批量同步和封面上传都会带上该身份，避免所有者发授权码后普通使用者的数据落到所有者账号。验证：node --test tests/flywheel-cover-asset-upload.test.mjs tests/flywheel-sync-quality.test.mjs 通过 8 个用例；npm run build 通过，保留既有 webpack bundle 体积 warning。

2026-05-04
- 工作台任务页自动回收增强完成：插件会持久记录自己为工作台任务打开的采集页，后台服务休眠恢复后仍能在任务结束时关闭对应页面；用户原本打开并被复用的页面不会被误关。验证：node --test tests/workbench-task-execution-cleanup.test.mjs tests/navigation-orchestrator.test.mjs 通过 9 个用例；npm run build 通过，保留既有 webpack bundle 体积 warning。

2026-05-07
- detail-probe 数据采集竞态修复完成：
  根因：_waitForNoteDataStable() 在 AJAX 填充 commentCount/imageList 前就判定数据稳定，
        导致评论、收藏、封面等字段采集为空。
  修复三层防御：
  ① 插件 isCollectedNoteUsable() 增加严格模式（要求完整统计数据+有效媒体）
  ② 插件 _waitForNoteDataStable() 增加 _isNoteDataComplete() 检查，超时延长至 6s
  ③ 工作台 buildObservationDraft() 增加 rawPayload 兜底提取（comments/coverImage）
  提交：插件 main c7e68db（2 files）；工作台修复恢复至 codex/bug 工作区待合并。
  验证：npm test 72/72 通过；npm run build 通过。
  待办：重载插件并触发监控轮次进行实机验证。
